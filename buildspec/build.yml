version: 0.2

env:
  variables:
    NODE_OPTIONS: "--max_old_space_size=8192"

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo "This is pre run"
      - ls -lart frontend/src
      - cd frontend && npm ci
      - echo "this is post run"
      - ls -lart frontend/src
      - npm run build:test
      - cd ../backend && npm ci

artifacts:
  files:
    - '**/*'
  base-directory: 'frontend/src'
